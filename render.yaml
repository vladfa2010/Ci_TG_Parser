services:
  # ─── Web Dashboard (FastAPI) ──────────────────────────────
  - type: web
    name: tgparser-web
    runtime: docker
    plan: starter
    branch: v2-multichannel
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tgparser-db
          property: connectionString
      - key: APP_MODE
        value: "web"
      - key: PORT
        value: "10000"

  # ─── Cron Job: многоканальный парсинг каждые 5 минут ──────
  - type: cron
    name: tgparser-cron
    runtime: docker
    plan: starter
    branch: v2-multichannel
    schedule: "*/5 * * * *"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tgparser-db
          property: connectionString
      # ─── Telegram API credentials ( Secrets ) ────────────
      - key: TG_API_ID
        sync: false
      - key: TG_API_HASH
        sync: false
      - key: TG_STRING_SESSION
        sync: false
      # ─── Channels & parsing config ──────────────────────
      - key: CHANNELS
        value: "markettwits"
      - key: HISTORY
        value: "0"
      - key: SCHEDULE_MODE
        value: "0"
      - key: MAX_CONCURRENT_CHANNELS
        value: "5"
      - key: RETRY_ATTEMPTS
        value: "3"

  # ─── Background Worker: планировщик (альтернатива cron) ───
  # Раскомментируй если хочешь заменить cron на постоянно
  # работающий background worker с SCHEDULE_MODE=1
  # - type: worker
  #   name: tgparser-scheduler
  #   runtime: docker
  #   plan: starter
  #   branch: v2-multichannel
  #   envVars:
  #     - key: DATABASE_URL
  #       fromDatabase:
  #         name: tgparser-db
  #         property: connectionString
  #     - key: TG_API_ID
  #       sync: false
  #     - key: TG_API_HASH
  #       sync: false
  #     - key: TG_STRING_SESSION
  #       sync: false
  #     - key: CHANNELS
  #       value: "markettwits"
  #     - key: SCHEDULE_MODE
  #       value: "1"
  #     - key: INTERVAL_SEC
  #       value: "300"
  #     - key: MAX_CONCURRENT_CHANNELS
  #       value: "5"

  # ─── Job: первоначальная загрузка истории (разовый запуск) ─
  # Запусти вручную из Render Dashboard → Jobs → New Job
  # Затем удали или отключи
  # - type: job
  #   name: tgparser-history
  #   runtime: docker
  #   plan: starter
  #   branch: v2-multichannel
  #   envVars:
  #     - key: DATABASE_URL
  #       fromDatabase:
  #         name: tgparser-db
  #         property: connectionString
  #     - key: TG_API_ID
  #       sync: false
  #     - key: TG_API_HASH
  #       sync: false
  #     - key: TG_STRING_SESSION
  #       sync: false
  #     - key: CHANNELS
  #       value: "markettwits"
  #     - key: HISTORY
  #       value: "1"
  #     - key: MAX_CONCURRENT_CHANNELS
  #       value: "3"

databases:
  - name: tgparser-db
    plan: free
    ipAllowList: []
